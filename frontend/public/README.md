`character.glb` here is your cleaned model — it loads automatically.

Once you have a rigged VRM, save it beside this file as `character.vrm`.
The app prefers the VRM whenever both are present.

You can also drag a `.glb` or `.vrm` straight onto the stage to try one
without restarting the dev server.
